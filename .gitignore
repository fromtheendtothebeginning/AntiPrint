# Python
backend/.venv/
__pycache__/
*.pyc

# 依赖与构建产物
node_modules/
frontend/dist/
dist/

# 本机配置与数据（含凭据，禁止提交）
backend/.env
backend/db_config.json
backend/data/
backend/log/
agent/config.json
agent/log/
agent/tmp/
agent/agent.lock

# 部署脚本（含服务器凭据，仅本机存在）
deploy*.bat
*deploy-config*

# 临时验证产物（测试脚本可留、截图不入库）
.tmp-test/shots/
.tmp-test/.admin-token.json   # 测试用的管理员令牌缓存，别提交

# 编辑器/系统
.vscode/
.idea/
Thumbs.db
desktop.ini
*.log

# 会话/计划产物
.zcode/

# 线上代理配置（含线上令牌，禁止提交）
agent/config.prod.json

# 本机/线上两套代理配置的备份（含令牌，禁止提交）
agent/config.local.json
